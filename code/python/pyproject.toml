[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yeastgem"
version = "0.0.1.dev0"
description = "Python interface to the yeast-GEM consensus genome-scale metabolic model"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "CC-BY-4.0" }
authors = [{ name = "SysBioChalmers", email = "info@sysbiochalmers.se" }]
keywords = ["systems biology", "metabolic model", "yeast", "Saccharomyces cerevisiae", "cobra"]

# Decision #1 was revised after phase 3: generic helpers MOVE to
# raven-python (Python) / RAVEN (MATLAB) rather than living locally
# in yeast-GEM. PORTING_PLAN.md reflects the change. yeast-GEM Python
# now depends on raven-python; the local yeastgem package is just the
# yeast-specific repo orchestration on top.
dependencies = [
    "raven-python @ git+https://github.com/SysBioChalmers/raven-python@feat/yeast-gem-shared",
    "cobra>=0.29",
    "pandas>=2.0",
    "pyyaml>=6.0",
    "matplotlib>=3.7",
    "numpy>=1.24",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4",
]

[project.urls]
"Source" = "https://github.com/SysBioChalmers/yeast-GEM"
"Issues" = "https://github.com/SysBioChalmers/yeast-GEM/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["yeastgem*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "RUF"]
